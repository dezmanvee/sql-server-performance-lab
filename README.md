# SQL Server Performance Lab

A lab project for documenting how to investigate slow queries, blocking, index opportunities, execution plans, and Query Store observations without exposing production data.

## Suggested case-study format
Problem -> Baseline -> Investigation -> Change -> Validation -> Lessons learned

Include screenshots only from a local lab database and remove all real data before publishing.
